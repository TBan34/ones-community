<!--ユーザー検索欄-->
<div class="SearchUserForm">
  <%= form_with url: admin_search_user_path, method: :get do |f| %>
    <%= f.text_field :search_user, placeholder: "ユーザー名or氏名を入力" %>
    <%= f.submit "検索", class: "btn btn-secondary btn-sm" %>
  <% end %>
</div>